openapi: 3.0.3
info:
  description: This is the PingData Configuration API for the Cipher Secret Key config object
  title: PingData Cipher Secret Key Config - OpenAPI 3.0
  version: "0.1"
externalDocs:
  description: PingDirectory Configuration API Docs
  url: https://docs.pingidentity.com/bundle/pingdirectory-91/page/ddl1564011495095.html
tags:
- description: Access cipher-secret-keys
  name: cipher-secret-key
paths:
  /cipher-secret-keys/{cipher-secret-key-name}:
    get:
      summary: Returns a single Cipher Secret Key
      operationId: get-cipher-secret-key
      parameters:
      - description: Name of the Cipher Secret Key to be read
        explode: false
        in: path
        name: cipher-secret-key-name
        required: true
        schema:
          type: string
        style: simple
      responses:
        "200":
          description: Successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/cipher-secret-key-response'
        "401":
          description: Requestor cannot be authenticated
        "403":
          description: Requestor does not have the required privileges
        "404":
          description: The Cipher Secret Key does not exist
      security:
      - basicAuth: []
      tags:
      - cipher-secret-key
    patch:
      summary: Update an existing Cipher Secret Key by name
      operationId: update-cipher-secret-key
      parameters:
      - description: Name of the Cipher Secret Key to be updated
        explode: false
        in: path
        name: cipher-secret-key-name
        required: true
        schema:
          type: string
        style: simple
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/updateRequest'
        description: Update an existing Cipher Secret Key
        required: true
      responses:
        "200":
          description: Successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/cipher-secret-key-response'
        "401":
          description: Requestor cannot be authenticated
        "403":
          description: Requestor does not have the required privileges
        "400":
          description: Invalid request
        "404":
          description: The Cipher Secret Key does not exist
      security:
      - basicAuth: []
      tags:
      - cipher-secret-key
components:
  schemas:
    cipher-secret-key-response:
      allOf:
      - $ref: '#/components/schemas/meta'
      - type: object
        properties:
          schemas:
            type: array
            minItems: 1
            maxItems: 1
            items:
              $ref: '#/components/schemas/Enumcipher-secret-keySchemaUrn'
          cipherTransformationName:
            type: string
            description: >-
                The algorithm name used to produce this cipher, e.g. AES/CBC/PKCS5Padding.
          initializationVectorLengthBits:
            type: integer
            description: >-
                The initialization vector length of the cipher in bits.
          keyID:
            type: string
            description: >-
                The unique system-generated identifier for the Secret Key.
          isCompromised:
            type: boolean
            description: >-
                If the key is compromised, an administrator may set this flag to immediately trigger the creation of a new secret key. After the new key is generated, the value of this property will be reset to false.
          symmetricKey:
            type: array
            items:
              type: string
            description: >-
                The symmetric key that is used for both encryption of plain text and decryption of cipher text. This stores the secret key for each server instance encrypted with that server's inter-server certificate.
          keyLengthBits:
            type: integer
            description: >-
                The length of the key in bits.
        required:
        - keyID
        - keyLengthBits
    Enumcipher-secret-keySchemaUrn:
      type: string
      enum:
      - "urn:pingidentity:schemas:configuration:2.0:cipher-secret-key"
    updateRequest:
      type: object
      properties:
        operations:
          type: array
          items:
            $ref: '#/components/schemas/Operation'
      required:
      - operations
    Operation:
      type: object
      properties:
        op:
          $ref: '#/components/schemas/EnumOperation'
        path:
          type: string
        value:
          type: string
      required:
      - op
      - path
    EnumOperation:
      type: string
      enum:
      - "add"
      - "remove"
      - "replace"
    meta:
      type: object
      properties:
        meta:
          type: object
          properties:
            resourceType:
              type: string
            location:
              type: string
        "urn:pingidentity:schemas:configuration:messages:2.0":
          type: object
          properties:
            notifications:
              type: array
              items:
                type: string
            requiredActions:
              type: array
              items:
                type: object
                properties:
                  property:
                    type: string
                  type:
                    type: string
                  synopsis:
                    type: string
                required:
                - type
                - synopsis
  securitySchemes:
    basicAuth:
      scheme: Basic
      type: http
